<!DOCTYPE html>
<html lang='en'>
<head>
  <title>TinyApp</title>
</head>
<body>
  <% include ./partials/_header.ejs %>
  <ul>
    <% for(let url in urls){ %>
    <li>
        <%= `Long URL: ${urls[url]} => Short URL: ${url}` %>
    </li>
      <br>
  <% } %>
  </ul>
  <p><a href='#'>Shorten New URL</a></p>
</body>
</html>